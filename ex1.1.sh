#!/bin/bash

# Write a script that creates a directory called bpl inside $HOME. Populate this
# directory with two subdirectories, bin and scripts.

cd $HOME
mkdir bpl
cd bpl
mkdir bin scripts

exit

